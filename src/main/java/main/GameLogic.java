package main;

/**
 * Created by Mike P on 10/2/2016.
 */
public class GameLogic {
/*
    public boolean validMove(Card checkHere, String directionCheck){
        if(this.getSuit() != checkHere.getSuit() || checkHere.getRank() ==  Card.Rank.King || checkHere.getRank() ==  Card.Rank.ZERO)
            return false;
        else{
            if(directionCheck.equals("Right")) {
                if (this.getRank() == ranks[checkHere.getRank().ordinal() - 1])
                    return true;
            }
            else if(directionCheck.equals("Left")) {
                if (this.getRank() == ranks[checkHere.getRank().ordinal() + 1])
                    return true;
            }
        }
        return false;
    }

    */
}
